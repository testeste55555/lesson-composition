# Privacy / プライバシー方針

このリポジトリは公開リポジトリです。プライバシー保護を最優先し、実際の授業ファイル・学習者情報・個人情報をGitHubへ保存しないことを前提に運用します。

## 1. 授業ファイルの扱い

既存授業検証モードでユーザーが選択した PPTX / DOCX / XLSX / PDF / ZIP / HTML / TXT 等は、ブラウザ内でのみ読み取ります。

- アプリから外部解析APIへアップロードしません。
- ファイル本文を `localStorage` / `IndexedDB` 等へ保存しません。
- 解析後は、判定に必要な特徴・活動ブロック候補のみを一時的に保持します。
- ファイル名も判定用データとして永続保存しません。
- ページ再読み込み・モード変更で一時データは破棄されます。

GitHub Pages自体への通常のWebアクセスでは、ホスティング基盤に通常の通信メタデータが届く可能性がありますが、ユーザーが選択した授業ファイルの本文を本アプリがGitHubへ送信する設計にはしません。

## 2. リポジトリへコミットしないもの

`.gitignore` で以下を原則除外します。

- Officeファイル、PDF、圧縮ファイル
- 画像・音声・動画など個人情報を含み得る媒体
- `private/`, `uploads/`, `input/`, `local/` 等の作業ディレクトリ
- `.env`、秘密鍵、認証情報、トークン
- ローカル解析結果・エクスポート

公開が必要な画像・音声等は、内容を人が確認したうえで `assets/public/` にのみ配置します。

## 3. 禁止する情報

次の情報を実データのまま公開リポジトリへコミットしません。

- 学習者・講師・取引先等の氏名
- 顔写真、在留カード、身分証、署名
- 電話番号、メールアドレス、住所
- 学籍番号、社員番号、在留番号等の識別子
- 成績、評価、相談記録、出欠、健康情報
- 個別企業・団体に関する非公開情報
- 秘密APIキー、パスワード、アクセストークン、リフレッシュトークン、サービスアカウント鍵

## 4. 実装上の原則

- 外部解析APIやアクセス解析を追加する場合は、事前に明示的な検討を行う。
- CDNや外部スクリプトへの依存は原則避ける。
- Content Security Policy で通信先を必要最小限へ限定する。
- 自動解析は推定扱いとし、教師によるHuman Gateを残す。
- 本文や個人情報をログへ出力しない。

### Human Gate共有同期の限定例外

2名の教師がHuman Gateの判定結果を共有するため、Firebase Authentication / Cloud Firestoreを使用する構成を許可します。ただし、初期状態では無効とし、Firebase側の認証・メンバー制限・Security Rulesを設定した後にのみ有効化します。

同期対象は次に限定します。

- candidate ID
- 語彙
- カテゴリー
- 採用 / 修正採用 / 不採用の判定
- 修正文
- Firebase Authentication UID（氏名・メールアドレスの代わりに使う技術的識別子）
- 判定日時

学習者情報、講師氏名・メールアドレス、授業ファイル、出欠、成績、健康情報、企業の非公開情報は同期しません。認証用メールアドレスとパスワードはFirebase Authenticationへ直接送信し、アプリの永続領域には保存しません。

Firebase Web API keyはブラウザ向け公開クライアント設定であり、サービスアカウント鍵等の秘密情報とは区別します。使用する場合は対象API・GitHub Pagesのreferrer制限等を可能な範囲で設定し、秘密鍵・Admin SDK認証情報・アクセストークン等は引き続きコミット禁止とします。

## 5. 誤コミット時

個人情報や秘密情報を誤ってコミットした場合、単に最新コミットから削除するだけではGit履歴に残ります。公開停止・履歴からの削除・必要に応じた認証情報の失効を優先して実施します。
